function Navbar() {
  return (
    <header className="navbar">
      <h1>Movie App</h1>

      <nav>
        <a href="#">Home</a>
        <a href="#">Movies</a>
        <a href="#">Watchlist</a>
      </nav>
    </header>
  );
}

export default Navbar;
